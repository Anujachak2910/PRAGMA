# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9         # Required for form/file uploads

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36              # 2.0.36+ required for Python 3.13 compatibility
alembic==1.13.1

# ─── Validation & Settings ────────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.7.1        # .env loading via BaseSettings

# ─── AI / LLM ─────────────────────────────────────────────────────────────────
httpx==0.27.0                   # HTTP client for Ollama API calls
anthropic==0.28.0               # Legacy SDK — app runs fully offline without an API key

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.32.3                 # HTTP client for feed ingestion and circular fetching
feedparser==6.0.11               # RSS/Atom feed parsing for RBI, SEBI, MCA feeds

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                   # TestClient for FastAPI
pytest-cov==5.0.0               # Test coverage reports
