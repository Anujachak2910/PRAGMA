# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9         # Required for form/file uploads

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36              # ORM — works with SQLite and PostgreSQL
alembic==1.13.1                 # Migrations (SQLite-compatible)
# psycopg2-binary               # Only needed if DATABASE_URL is postgresql://
                                # Uncomment for development with Neon/Postgres

# ─── Validation & Settings ────────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.7.1        # .env loading via BaseSettings

# ─── HTTP Client (used by Ollama service) ─────────────────────────────────────
httpx==0.27.0

# ─── Offline Document Parsing (PDF / DOCX — no internet required) ─────────────
pymupdf==1.24.10                # PyMuPDF — fast PDF text extraction (local only)
python-docx==1.1.2              # DOCX parsing (local only)

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# ─── REMOVED (internet / cloud dependencies) ──────────────────────────────────
# anthropic==0.28.0    — Claude API SDK (requires internet + API key)
# psycopg2-binary      — PostgreSQL driver (not needed for SQLite)
# requests==2.32.3     — Used only for feed ingestion (not called in demo)
# feedparser==6.0.11   — RSS/Atom parsing for RBI feeds (not called in demo)
